server:
    port: 8080

spring:
    application:
        name: card-words

    datasource:
        url: jdbc:postgresql://localhost:5432/card_words
        username: postgres
        password: 123456
        driver-class-name: org.postgresql.Driver

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: false # Tắt hiển thị SQL
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true
                use_sql_comments: true
        open-in-view: false

    # Redis Configuration
    data:
        redis:
            host: localhost
            port: 6379
            password:
            timeout: 60000

    # Cache Configuration
    cache:
        type: redis
        redis:
            time-to-live: 3600000 # 1 hour in milliseconds

    mail:
        host: smtp.gmail.com
        port: 587
        username: thuanthichlaptrinh@gmail.com
        password: jcdfsqryziswpjfw
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true
                    ssl:
                        trust: smtp.gmail.com
                    connectiontimeout: 5000
                    timeout: 3000
                    writetimeout: 5000

springdoc:
    api-docs:
        enabled: true
    swagger-ui:
        enabled: true

# JWT Configuration
jwt:
    secret-key: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    expiration: 86400000 # 24 hours in milliseconds
    refresh-token:
        expiration: 604800000 # 7 days in milliseconds

activation:
    expired-time: 86400000 # 24 hours in milliseconds
    resend-interval: 300000 # 5 minutes in milliseconds

logging:
    level:
        org:
            springframework:
                security: trace
        com:
            thuanthichlaptrinh: error

http:
    request-logging-filter:
        enabled: true
        include-payload: true
