-- V1: Initial Schema (Empty - Schema already exists from Hibernate)
-- This migration is a placeholder because the schema was created by Hibernate before Flyway was enabled
-- All tables, indexes, and constraints already exist in the database

-- Mark this migration as successful without making any changes
SELECT 1;
