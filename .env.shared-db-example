# .env.shared-db-example
# Cấu hình khi dùng chung 1 database server (Cloud/VPS)

# ============================================
# SHARED DATABASE CONFIGURATION
# ============================================
# Thay vì chạy PostgreSQL trên mỗi máy,
# tất cả các máy kết nối đến 1 database server

# PostgreSQL Server (Cloud/VPS)
POSTGRES_HOST=your-database-server.com  # Hoặc IP: 203.162.4.190
POSTGRES_PORT=5432
POSTGRES_DB=card_words_shared
POSTGRES_USER=cardwords_app
POSTGRES_PASSWORD=StrongPassword@2025

# Redis (có thể dùng Redis Cloud hoặc local)
REDIS_HOST=localhost  # Hoặc Redis Cloud
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=  # Nếu Redis có password

# Spring Boot
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8080

# JWT Secret (PHẢI GIỐNG NHAU trên tất cả máy)
JWT_SECRET=Y2FyZC13b3Jkcy1zZWNyZXQta2V5LWZvci1qd3QtdG9rZW4tZ2VuZXJhdGlvbg==
JWT_EXPIRATION=86400000

# Google OAuth (PHẢI GIỐNG NHAU)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8080/api/v1/auth/oauth2/callback/google

# Firebase
FIREBASE_PROJECT_ID=your-project
FIREBASE_STORAGE_BUCKET=your-project.appspot.com
FIREBASE_DATABASE_URL=https://your-project.firebaseio.com

# ============================================
# DOCKER COMPOSE CONFIGURATION
# ============================================
# Khi dùng shared database, chỉ cần chạy:
# - Spring Boot app (connect to remote DB)
# - Redis local (cho cache)
#
# KHÔNG CẦN chạy PostgreSQL container nữa!
